version: "3.5"
services:
  vm_box:
    command:
      - bash
      - -c
      - sed -i '/neighbor fabric capability extended-nexthop/ {a\
        neighbor fabric ebgp-multihop 64
        }' /opt/ispsystem/vm/scripts/kvm/etc/frr_routereflector_bgpd.tmpl